enum UserType { student, faculty, coaching_center, admin }

enum Gender { male, female, other }

enum CourseLevel { beginner, intermediate, advanced }

enum InstructorType { faculty, coaching_center }

enum EnrollmentType { course, live_class }

enum EnrollmentStatus { active, completed, dropped, paused }

enum PaymentStatus { pending, paid, refunded }

enum LiveClassStatus { scheduled, live, completed, cancelled }

enum AdminRole { super_admin, content_admin, support_admin, marketing_admin }

enum VerificationStatus { pending, approved, rejected }
