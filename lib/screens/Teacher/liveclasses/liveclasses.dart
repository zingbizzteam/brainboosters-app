import 'package:flutter/material.dart';

class LiveClasses extends StatelessWidget {
  const LiveClasses({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Leaderboard Page'));
}
